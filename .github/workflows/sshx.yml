name: OP

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 345
    steps:
      - name: Start sshx session
        run: |
          curl -sSf https://sshx.io/get | sh -s run &
          sleep 20700
